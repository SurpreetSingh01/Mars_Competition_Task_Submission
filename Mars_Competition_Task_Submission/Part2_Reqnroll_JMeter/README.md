# Part 2 Submission

This folder is a clean Part 2 package assembled from the repo.

Included:
- manual testing workbook with an `A2_A16_Tracker` sheet and the detailed source cases
- Reqnroll + Selenium C# framework without `bin`, `obj`, generated feature code, or runtime artifacts
- JMeter test plan and the compiled performance analysis workbook

Notes:
- `credentials.json` in the submission copy is sanitized on purpose
- runtime outputs such as `Artifacts`, `bin`, `obj`, JMeter reports, and temporary files are excluded from this package
